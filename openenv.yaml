name: ticket-routing-env
description: AI environment for customer support ticket routing
version: 1.0

observation:
  type: object
  properties:
    ticket_text:
      type: string
    task_type:
      type: string

action:
  type: object
  properties:
    department:
      type: string
    priority:
      type: string
    escalation:
      type: string

reward:
  type: float
